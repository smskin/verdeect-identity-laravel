<?php

declare(strict_types=1);

use Verdeect\IdentityIntegration\Tests\TestCase;

/*
|--------------------------------------------------------------------------
| Прогон пакета интеграции
|--------------------------------------------------------------------------
|
| Наборы идут в контейнере Laravel, поднятом testbench: пакет живёт внутри
| приложения, и проверять его вне контейнера нечем.
|
| `RefreshDatabase` не подключается — базы у пакета нет. Обращения к установке
| перехватывает `Http::fake()` оснасткой из `testing/helpers.php`, которая
| приходит автозагрузкой composer и потому доступна и наборам продукта.
|
*/

pest()->extend(TestCase::class)->in('Feature', 'Unit');
